name: Release PDCA Skill

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.11"

      - name: Install test dependencies
        run: uv sync --extra test
        working-directory: claude-skill

      - name: Run tests
        run: bash claude-skill/run-tests.sh

      - name: Build skill package
        run: bash claude-skill/build-skill.sh

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: claude-skill/pdca-framework.skill
          generate_release_notes: false
          body: |
            ## Install via Claude UI

            1. Download `pdca-framework.skill` below
            2. Go to claude.ai → **Customize** → **Skills** → **Add Skill**
            3. Upload the file and toggle it on

            **pdca-framework.skill** — TDD-disciplined AI-assisted code generation with Beads integration.

            To generate a custom AI workflow skill for any domain, see [ai-workflow-builder](https://github.com/kenjudy/ai-workflow-builder).

            See the [README](https://github.com/kenjudy/pdca-framework/blob/main/claude-skill/README.md) for full documentation.
