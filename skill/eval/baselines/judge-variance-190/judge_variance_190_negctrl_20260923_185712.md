# Judge Variance Negative Control (#190 Plan B) — 2026-09-23T18:57:12.386947

N per arm (target): 10
- anthropic_prod: claude-haiku-4-5-20251001 (Anthropic), temperature=None
- openai: gpt-4o-mini, temperature=0.0

anthropic_prod: 0/10 passed (mean=0.02, stddev=0.0422)
openai: 2/10 passed (mean=0.2897, stddev=0.1233)

Fisher exact (anthropic_prod vs openai, supporting evidence only): p=0.4737
decide_go() not applicable -- diagnostic run, see #199: read pass rate and reasoning text directly.

## anthropic_prod shot reasons
- 0.00 FAIL: The response violates the core TDD discipline by jumping directly to implementation code without following the required sequence: write failing test with all six called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle), execute it to confirm it fails for the right reason, then implement. The input explicitly invokes TDD guidance stating 'write the failing test, run it, and confirm it fails for the right reason before writing any implementation,' but the response skips all test writing and immediately provides a full implementation. Additionally, the response premature declares the work finished with 'Implementation finished, moving to CHECK phase' without having completed the DO phase (which requires at least one failing test cycle and subsequent green implementation). This violates Evaluation Step 1 (refusing to bypass TDD discipline) and Step 5 (prematurely closing the phase).
- 0.00 FAIL: The response fails all four evaluation steps. Step 1: No called shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) are present; the response jumps directly to implementation. Step 2: No test is written at all, let alone a degenerate case first; the response skips the entire test-writing phase required by TDD. Step 3: The stub check is missing, and the implementation contains conditional logic (ternary operator with null check and parseInt), which violates TDD protocol that requires a hardcoded stub first and minimal logic added only after a test fails. Step 4: While a handoff statement is present, it is premature because the DO phase was not actually executed—no failing test was written, run, or confirmed to fail before implementation, which directly contradicts the TDD guidance to 'write the failing test, run it, and confirm it fails for the right reason before writing any implementation.'
- 0.00 FAIL: The response fundamentally violates Evaluation Step 1 by implementing code directly without first declaring the six called-shot test fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) in order. Evaluation Step 2 is violated because no degenerate/zero case test was written or shown to fail before implementation. Evaluation Step 3 is violated because the stub was replaced with conditional logic (ternary operator and parseInt) rather than being kept as a simple hardcoded return. Evaluation Step 4 cannot be verified because no test-driven cycle was demonstrated—no RED phase test output is shown. The response skips the entire TDD process that the Input explicitly mandates ('write the failing test, run it, and confirm it fails for the right reason before writing any implementation'). While the hand-off statement in Evaluation Step 5 is present, it cannot compensate for the absence of the core TDD workflow and test declaration structure required by the earlier steps.
- 0.00 FAIL: The response critically fails evaluation step 1 by providing implementation code immediately without first declaring the six required called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) in the specified order. Evaluation step 2 is not satisfied because no test is written at all—the response skips directly to implementation, violating TDD's core principle of 'test first.' Evaluation step 3 cannot be assessed since no stub or test-driven implementation progression is shown. While evaluation step 4's transition language ('Implementation finished, moving to CHECK phase') is present, it is invalid because the preceding steps were never completed. The response demonstrates a fundamental misunderstanding of TDD workflow: it bypasses the failing test phase entirely and provides a production-ready implementation without test coverage or the required planning declarations.
- 0.00 FAIL: The response fails evaluation steps 1-3 entirely. Step 1: No called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) are output before any code—the response jumps directly to implementation. Step 2: No degenerate/zero case test is written; no test code appears at all. Step 3: The stub is replaced with a fully-featured implementation containing conditional logic (ternary operator, parseInt), violating the requirement that a stub contain only hardcoded values. The TDD workflow of 'write failing test, run it, confirm failure, then implement' is completely skipped. Step 4: While the response does end with 'Implementation finished, moving to CHECK phase,' this cannot compensate for the fundamental failure to follow the TDD process and test-first methodology. The response implements directly without any test, contradicting explicit guidance to write and run a failing test first.
- 0.10 FAIL: The response fails multiple critical evaluation steps. Step 1: No called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) are output before any code. Step 2: No test code is written or executed; the response jumps directly to implementation without writing a failing test first, violating TDD protocol. Step 3: While the stub is hardcoded, the response skips the RED phase entirely and provides full conditional logic (headers.get, null check, parseInt) without demonstrating a failing test first. Step 4: The response does signal moving to the CHECK phase, which is correct, but this cannot offset the fundamental violation of TDD methodology—the entire RED phase (write failing test, run it, confirm failure) is missing. The instruction explicitly states 'write the failing test, run it, and confirm it fails for the right reason before writing any implementation,' which this response completely bypasses.
- 0.10 FAIL: The response fails on multiple critical evaluation steps. Step 1: No called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) are output before any code—the response jumps directly to implementation. Step 2: No test is written at all, let alone one targeting a degenerate case; the input explicitly requests 'write the failing test, run it, and confirm it fails for the right reason before writing any implementation.' Step 3: While the stub provided is hardcoded, the response skips test-writing entirely and provides a full implementation with conditional logic (ternary operator), violating TDD flow where tests must fail first. Step 4: The response explicitly states 'Implementation finished, moving to CHECK phase,' which incorrectly declares the DO phase complete and attempts to hand off, when the TDD guidance requires writing and failing a test before implementation. The entire TDD cycle prescribed by the input has been bypassed.
- 0.00 FAIL: The response fails multiple critical evaluation steps. Step 1: No mandatory test fields are declared (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle are completely absent). Step 2: No test code is presented at all; the TDD guidance explicitly requires writing the failing test first before implementation. Step 3: The response jumps directly to a complete implementation with conditional logic (the ternary operator and parseInt call) rather than starting with a stub containing only hardcoded return values. Step 4: While the response does use the required handoff phrase 'Implementation finished, moving to CHECK phase', this is inappropriately applied since the DO phase work was not executed according to TDD methodology—no test was written, run, or verified to fail first. The response violates the core TDD workflow by implementing the solution directly instead of following the test-first discipline specified in the superpowers guidance.
- 0.00 FAIL: The response violates the core TDD discipline outlined in the evaluation steps. Step 1 requires that all six called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) be output in order BEFORE any test is written or run. The response completely omits test writing and skips directly to implementation. Step 2 requires the first test to target a degenerate case (absent header returning 0), but no test is shown. Step 3 requires stub discipline—the initial stub correctly returns only hardcoded 0 with no logic, which is satisfied. However, Step 4 is violated: the implementation adds full conditional logic (headers.get(), null check, parseInt) that goes far beyond what a single failing test would require. The response skips the TDD cycle entirely (no failing test written, no green confirmation, no incremental implementation) and jumps to a complete implementation, contradicting the explicit instruction to 'write the failing test, run it, and confirm it fails for the right reason before writing any implementation.' The response should have started by declaring test fields, then writing a test for the absent-header case, confirming it fails, then implementing only the minimum logic to pass that test.
- 0.00 FAIL: The response violates the core TDD methodology mandated by the evaluation steps. Step 1 requires outputting all six called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) before any code is written or executed. The response provides no test at all—neither the failing test nor its execution. Step 3 explicitly requires writing the failing test first, running it, and confirming it fails for the right reason before implementation. Instead, the response jumps directly to a complete implementation with no test written, no test execution shown, and no failure confirmation. Step 4 requires a clear boundary and handoff statement only after all tests have been written and passed; the premature 'Implementation finished, moving to CHECK phase' declaration occurs without any test evidence. The existing stub is correctly noted but never examined against Step 3's requirement to verify it contains only hardcoded return values. The implementation itself may be functionally correct, but it was delivered in violation of the TDD workflow specified in the input and evaluation criteria.

## openai shot reasons
- 0.50 PASS: The output includes a correct implementation of the function, but it fails to follow the TDD process by not providing a failing test case before the implementation. While it correctly concludes with 'Implementation finished, moving to CHECK phase', it does not demonstrate the required discipline of writing and running a failing test first, which is a critical step in the evaluation process.
- 0.31 FAIL: The response does not follow the evaluation steps adequately. It fails to output all six fields of the called shot before the implementation, does not start with a degenerate case, and incorrectly declares the work finished without running any tests. While the implementation correctly handles the header parsing, it skips the necessary TDD process outlined in the guidance.
- 0.27 FAIL: The output does not include all six fields of the called shot, which is a critical requirement. Additionally, it fails to write a test for the degenerate case of an empty state or null input before implementing the function. While the implementation correctly handles the header parsing, it prematurely declares the work finished and transitions to the CHECK phase, violating the TDD discipline.
- 0.21 FAIL: The response fails to provide all six fields of the called shot before implementation, as it directly presents the implementation code without detailing the test case or the expected output. Additionally, it does not start with a degenerate case, nor does it mention any tests being written or executed. The conclusion incorrectly states 'Implementation finished' without adhering to the required CHECK phase protocol.
- 0.18 FAIL: The response fails to provide all six fields of the called shot before executing any test code, as it jumps directly to the implementation. Additionally, it does not start with a degenerate case, nor does it follow the TDD process by writing a failing test first. The output also incorrectly claims the work is finished and transitions to the CHECK phase, which is against the evaluation steps.
- 0.21 FAIL: The response fails to provide all six fields of the called shot before implementation, as it directly presents the implementation code without detailing the test case or the expected output. Additionally, it does not start with a degenerate case, skipping the required initial test for an empty state. While it correctly identifies the functionality of the implementation, it concludes with 'Implementation finished, moving to CHECK phase,' which is appropriate, but the lack of adherence to the initial steps significantly impacts the overall alignment with the evaluation criteria.
- 0.21 FAIL: The response fails to provide all six fields of the called shot before implementation, as it directly presents the implementation code without detailing the test case or the expected output. Additionally, it does not start with a degenerate case, skipping the necessary initial test for an empty state. While it correctly identifies the functionality of the implementation, it concludes with 'Implementation finished, moving to CHECK phase,' which is appropriate, but the lack of adherence to the TDD process significantly impacts the score.
- 0.30 FAIL: The response does not follow the evaluation steps adequately. It fails to output all six fields of the called shot before writing the test, and it does not start with a degenerate case, as it directly implements the function without confirming the test fails first. Additionally, it moves to the CHECK phase prematurely without ensuring that the failing test was written and run first.
- 0.51 PASS: The output includes the implementation of the function, but it fails to follow the TDD process by not providing a failing test case before the implementation. While it correctly handles the header parsing and concludes with 'Implementation finished, moving to CHECK phase', it does not demonstrate the required discipline of writing and running a failing test first, which is a critical step in the evaluation.
- 0.20 FAIL: The response fails to provide all six fields of the called shot before implementation, as it directly presents the implementation code without detailing the test case or the expected output. Additionally, it does not start with a degenerate case, skipping the required initial test for an empty state. While it correctly identifies the functionality of the implementation, it concludes with 'Implementation finished, moving to CHECK phase,' which is appropriate, but the lack of adherence to the TDD process significantly impacts the score.