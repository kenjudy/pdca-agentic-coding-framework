# Judge Variance Case2 (2-first-step) (#190 Plan B) — 2026-09-23T15:44:28.507508

N per arm (target): 10
- anthropic_prod: claude-haiku-4-5-20251001 (Anthropic), temperature=None
- openai: gpt-4o-mini, temperature=0.0

anthropic_prod: 8/10 passed (mean=0.6, stddev=0.2211)
openai: 10/10 passed (mean=0.6905, stddev=0.0876)

Fisher exact (anthropic_prod vs openai, supporting evidence only): p=0.4737
Pre-registered go/no-go: GO (directional -- see module docstring)

## anthropic_prod shot reasons
- 0.60 PASS: The response demonstrates strong TDD discipline through Step 1: it outputs all six called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) before any code execution for Tests 1, 2, and 4. Step 2 is satisfied: Test 1 targets the degenerate case (nil event_type) rather than happy path. Step 3 is partially met: the stub contains only `class WebhookDelivery < ApplicationRecord; end` with no branching, and subsequent GREEN phases add validations directly demanded by failing tests (no over-implementation detected in visible sections). However, Step 4 is critically unmet: the response does not include an explicit handoff statement like 'Implementation finished, moving to CHECK phase' or equivalent. The response ends mid-test (Test 4 is incomplete with truncated code), suggesting the work is ongoing rather than explicitly declared as complete. This omission violates the evaluation requirement for an unambiguous phase transition declaration.
- 0.20 FAIL: The response violates evaluation steps 1 and 4 critically. Step 1 requires all six called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) to appear in order before any test code. While the response labels these fields for Tests 1–4, the actual test code begins appearing immediately after the first called-shot section (RED Phase — Test 1 starts before Test 2's called-shot fields are complete). Step 4 requires an explicit handoff statement declaring the DO phase complete (e.g., 'Implementation finished, moving to CHECK phase'). The response ends abruptly mid-Test 4 with no such declaration. Additionally, the response is incomplete—it cuts off mid-sentence during Test 4's code block, making it impossible to evaluate whether the stub correctly contains only hardcoded values without conditional logic (Step 3) or whether all six fields were properly presented for every test.
- 0.70 PASS: The response demonstrates strong TDD discipline by declaring all six required fields in the CALLED SHOT format before writing test code (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle). It correctly identifies the degenerate case (missing event_type) as Test 1 and properly sequences validation tests. The stub implementation shows only a bare class with no conditional logic. However, the response fails the hand-off requirement: it does not include an explicit declaration such as 'Implementation finished, moving to CHECK phase' at the natural end of the DO phase. Additionally, the output appears truncated mid-Test 4 ('is valid with event_type and a payload hash'), leaving the test incomplete and preventing verification that all six fields were stated for subsequent tests. The architectural safety check and migration-first approach are excellent, but the incomplete state and missing phase transition declaration prevent a higher score.
- 0.70 PASS: The response strongly aligns with TDD discipline and the evaluation steps. Step 1 is fully satisfied: all six called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) are explicitly output for Tests 1, 2, 3, and 4 before any code execution. Step 2 is met: the first test targets a degenerate case (event_type: nil) establishing the API boundary, not the happy path. Step 3 is verified: the initial stub (class WebhookDelivery < ApplicationRecord; end) contains no branching logic, and Test 1's assertion (presence validation) directly targets one branch; no over-implementation occurs. However, Step 4 is not fully satisfied: the response concludes with 'is valid with event_type and a payload hash' text cut off mid-test, without the explicit phrase 'Implementation finished, moving to CHECK phase.' or equivalent. The handoff statement naming the next phase is missing, which violates the requirement for a clear closing signal. The response demonstrates excellent TDD rigor (red-green cycles, migration-first approach, degenerate case priority) but fails the final gating criterion by not explicitly naming the phase transition.
- 0.60 PASS: The response demonstrates strong alignment with TDD DO Phase requirements through Step 3 but critically fails Step 4 (handoff verification). All four completed tests include the six mandatory called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) positioned before code. Tests follow degenerate/zero-case-first ordering (nil event_type → nil payload → type validation → persistence). Stub discipline is correctly applied: the initial stub contains only `class WebhookDelivery < ApplicationRecord; end` with no branching logic, and each green-phase implementation adds only the validations needed for the current test without anticipating later requirements. The response is thorough, pedagogically sound, and methodical. However, the output terminates mid-test (Test 4 is incomplete: 'it is valid with event_type and a payload hash do' cuts off at parameter setup) and provides no explicit handoff statement such as 'Implementation finished, moving to CHECK phase.' or equivalent. Step 4 explicitly requires an unambiguous handoff declaration—not merely 'complete' or 'done'—and the response provides neither completion nor handoff, leaving the DO Phase work in an undefined state.
- 0.20 FAIL: The response demonstrates partial understanding of TDD structure but fails critical evaluation steps. Step 1: While the response lists six called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle), it does NOT output them before ANY test code is written—test code and migration code appear immediately after the architectural check, violating the strict sequencing requirement. Step 2: The first actual test correctly targets a degenerate case (nil event_type), which is appropriate. Step 3: The stub phase is acceptable—a bare `class WebhookDelivery < ApplicationRecord; end` with no conditional logic—but the response adds validations incrementally in subsequent greens, which is correct. Step 4: The response is incomplete and truncates mid-test (cuts off at 'it is valid with event_type and a payload hash'), never reaching the explicit handoff statement 'Implementation finished, moving to CHECK phase.' Required conclusion is missing entirely. Additionally, Step 1's 'Oracle' field is never explicitly stated in the output, only implicit in expectations. The response shows strong conceptual alignment with TDD and PDCA phases but fails structural compliance with the mandatory format and incompleteness.
- 0.80 PASS: The response demonstrates strong TDD discipline by declaring all six called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) before writing any test code. Tests 1–3 correctly target degenerate/boundary cases (nil event_type, nil payload, non-hash payload) rather than happy path. The stub for Test 1 is appropriately minimal (empty class), and subsequent implementations add only conditional logic necessary for each failing test (presence validations, then custom hash validation). The architectural safety check and migration-first approach are sound. However, the response is incomplete—it cuts off mid-Test 4 without executing the test or providing the final handoff phrase 'Implementation finished, moving to CHECK phase.' This truncation prevents full verification of step 4 (proper termination signal) and means tests 4–6 lack the called-shot declarations and execution verification that would confirm adherence to the full TDD cycle. The foundation is excellent but the execution is unfinished.
- 0.70 PASS: The response demonstrates strong TDD discipline by outputting all six required called-shot fields before any test code runs (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle). Tests 1 and 2 correctly target degenerate cases (nil event_type, nil payload) and validate the sequencing. The stub implementation (class WebhookDelivery < ApplicationRecord; end) contains only hardcoded behavior with no conditional logic, and subsequent validations are added minimally and only as needed to make each test pass. However, the response is incomplete: it cuts off mid-sentence during Test 4's called shot and never reaches the required explicit handoff statement ('Implementation finished, moving to CHECK phase' or equivalent). The response also skips Test 3 (blank string) with explicit justification, which is acceptable TDD reasoning but leaves ambiguity about whether the full cycle was intended. The architectural safety check and migration-first approach are solid practices, but the truncation prevents full evaluation of whether all six tests achieve green status and whether a proper phase completion statement exists.
- 0.80 PASS: The response demonstrates strong TDD discipline by outputting all six called-shot fields (Test name, Behavior under test, Expected failure, Why this test first, Stub check, Oracle) in order before any code is written for Tests 1–3. Fields are substantive and test-specific, not generic placeholders. Test 1 correctly targets a degenerate case (nil event_type). The stub contains only hardcoded class definition with no conditional logic. GREEN phase logic (validates :event_type, presence: true) directly satisfies the failing test without over-implementing. However, the response is incomplete: it cuts off mid-sentence during Test 4 (happy path) and never reaches a handoff phrase like 'Implementation finished, moving to CHECK phase.' The truncation prevents verification that the response avoids claiming the work is complete and properly transitions to the next PDCA phase. Additionally, Test 3 decision (to skip blank string validation as vacuously green) is reasonable but could have been more explicitly justified with reference to Rails documentation.
- 0.70 PASS: The response demonstrates strong TDD discipline in most areas: it outputs all six called-shot fields for Tests 1–4 in proper order (test name, behavior under test, expected failure, why this test first, stub check, oracle), starts with a degenerate case (nil event_type), implements migrations before model code, and adds only the validation logic required by each failing test without over-implementing. The stub correctly contains no conditional logic, and subsequent GREEN phases add only presence and hash-type validations. However, the response is incomplete: Test 4 is cut off mid-execution (the actual assertion is missing after 'delivery = WebhookDelivery.new('), and no indication is given of whether remaining tests (round-trip payload, etc.) will follow or have been completed. The output also claims to work out 'all cases upfront' but then dismisses Test 3 (blank event_type) as 'vacuously green,' which is correct but leaves ambiguity about the final test suite. The response does not declare the work finished, which is good, but the truncation and lack of closure on the full cycle create uncertainty about whether the full TDD cycle has actually been executed.

## openai shot reasons
- 0.66 PASS: The response includes a clear outline of the test cases and follows TDD principles by starting with a degenerate case. However, it lacks a complete implementation of the initial stub and does not explicitly show the refusal to skip tests, which is crucial for maintaining discipline in TDD. Additionally, while it confirms the necessary fields and file locations, it could better emphasize the hardcoded nature of the initial stub during the RED phase.
- 0.66 PASS: The response includes a detailed breakdown of the test cases and adheres to TDD principles by starting with degenerate cases and ensuring validations are in place. However, it fails to explicitly declare the work finished and move to the CHECK phase, which is a key requirement. Additionally, while it lists the test cases, it does not output all six fields of the called shot before writing tests, which is another shortcoming.
- 0.81 PASS: The response effectively outlines the necessary steps for implementing webhook support, including confirming file locations and establishing a clean baseline. It also correctly identifies and lists the test cases upfront, adhering to TDD principles. However, it lacks a clear declaration of completion with 'Implementation finished, moving to CHECK phase,' which is a critical requirement. Additionally, while it starts with a degenerate case, it could have emphasized the refusal to skip tests more explicitly.
- 0.82 PASS: The response includes all six fields of the called shot and starts with a degenerate case, validating the API's existence. It maintains TDD discipline by refusing to skip tests and correctly implements stubs. However, it could improve by explicitly stating the expected failures for each test case in the initial outline, which would enhance clarity on the testing strategy.
- 0.65 PASS: The response includes a detailed test list and correctly identifies the degenerate case for the first test. However, it fails to explicitly state that it will not skip tests, which is crucial for TDD discipline. Additionally, it does not conclude with 'Implementation finished, moving to CHECK phase', which is necessary to indicate the completion of the implementation phase.
- 0.73 PASS: The response includes all six fields of the called shot and starts with a degenerate case test, establishing the API correctly. It maintains TDD discipline by not skipping tests and emphasizes the importance of validation. However, it does not conclude with the required phrase indicating the transition to the CHECK phase, which slightly detracts from its alignment with the evaluation steps.
- 0.63 PASS: The response includes a detailed outline of the test cases and follows the TDD process, but it fails to explicitly state the initial degenerate case before any code is written. While it does provide a comprehensive list of tests, it does not clearly indicate that it is starting with a degenerate case, which is a key evaluation step. Additionally, it does not conclude with the required phrase about moving to the CHECK phase, which is a significant shortcoming.
- 0.75 PASS: The response includes all six fields of the called shot and starts with a degenerate case, validating the API's initial state. It also correctly outlines the tests and their expected outcomes, adhering to TDD principles. However, it lacks explicit refusal to skip tests, as it mentions skipping a test that is vacuously green, which could imply a failure to demand the failing test first. Overall, it demonstrates strong alignment with the evaluation steps but has a minor shortcoming in the TDD adherence aspect.
- 0.54 PASS: The response includes a detailed outline of the test cases and follows TDD principles by starting with degenerate cases and ensuring validations are added correctly. However, it fails to explicitly state that it will not skip tests, which is a critical aspect of TDD discipline. Additionally, the output suggests a transition to the CHECK phase without clearly indicating that the work is not finished, which detracts from full alignment with the evaluation steps.
- 0.66 PASS: The response includes a clear outline of the test cases and follows TDD principles by starting with a degenerate case. However, it fails to explicitly declare the work as finished and does not include all six fields of the called shot before executing tests. The output also lacks a final statement indicating the transition to the CHECK phase, which is a critical requirement.
