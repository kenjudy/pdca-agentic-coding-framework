# PDCA Eval Report — 2026-09-17 00:10:11

**Environment:** deepeval: 4.2.2, anthropic: 1.4.0

**Rubric ladder:** 3429422579fa

## Summary

| Scenario | Phase | Score | Threshold | GEval | Mechanical |
|----------|-------|-------|-----------|-------|------------|
| 1b-clear-analysis | Planning | 0.90 | 0.50 | ✅ | ✅ |
| 1b-needs-refactoring | Planning | 0.90 | 0.50 | ✅ | ✅ |
| 1b-multi-system | Planning | 0.90 | 0.50 | ✅ | ✅ |
| 1b-critic-pass-warranted | Planning | 0.90 | 0.50 | ✅ | ✅ |

## Scenario Details

### 1b-clear-analysis (Planning)

**GEval:** 0.90 / threshold 0.50 ✅

**Mechanical checks:**

- ✅ `must_not_contain: 'def deliver_webhook'` — 'def deliver_webhook' absent (good) in output
- ✅ `must_not_contain: 'rails generate'` — 'rails generate' absent (good) in output

**Judge reasoning:**

> The Actual Output demonstrates strong alignment with all six evaluation steps. (1) Steps are numbered and atomic—each introduces one testable behavior (status field, HTTP delivery, job queueing, listener integration, integration test, docs). (2) A comprehensive test list is provided as a distinct planning artifact with golden path, degenerate cases (non-2xx, network errors), and exception scenarios, separate from implementation steps. (3) No refactor: steps are present, and the output explicitly confirms \"none needed,\" correctly bypassing the refactor-before-feat checkpoint. (4) No runnable code is included—method names, schema references, queue patterns, and ASCII diagrams are present as context, but no bodies or DSL blocks are copy-paste-ready. (5) Each step includes clear acceptance criteria and definition of done (e.g., \"migration runs cleanly, all tests pass\" in Step 1; \"HMAC verified against test vector\" in Step 2). (6) The plan spans 5+ files across models, services, jobs, listeners, and initializers, introducing a new architectural pattern (EventBus subscription + async delivery). The output explicitly raises the adversarial critic question (\"CHECK Criteria\" section and risk table) before implementation, satisfying this checkpoint. Minor weakness: the risk table and rollback section, while helpful, slightly exceed strict artifact requirements but add value without introducing implementation code.

**Input:**

```
Analysis complete. We need to add webhook delivery to our Rails app. WebhookDelivery model already exists (persists event_type and payload). We need to: (1) add HTTP delivery logic (POST to subscriber URL with HMAC signature), (2) add retry via existing Sidekiq worker pattern, (3) hook into existing EventBus subscription. No preparatory refactoring needed — existing abstractions fit cleanly.
```

**Model output:**

```
# Planning Phase: Webhook Delivery Implementation

Let me create a coherent, TDD-optimized implementation plan based on the analysis.

---

## Integration Map: End-to-End Data Flow

```
EventBus.publish(event_type, payload)
  └─> WebhookSubscriptionListener#handle (EventBus subscriber)
        └─> WebhookDelivery.create!(event_type:, payload:, subscriber:)
              └─> WebhookDeliveryJob.perform_later(delivery.id)  [Sidekiq]
                    └─> WebhookDeliveryService#call(delivery)
                          ├─> Build request (HMAC signature header)
                          ├─> POST to subscriber.url
                          ├─> delivery.mark_delivered! / .mark_failed!
                          └─> raise on failure → Sidekiq retry
```

**Touch points on existing code:**
- `EventBus` — add `subscribe` call (read-only discovery needed at DO time)
- `WebhookDelivery` — add status column + state transition methods
- Sidekiq worker pattern — new job following existing job conventions

---

## Test List (All Behaviors to Verify)

*Planning artifact — executed one test at a time in DO phase.*

**WebhookDeliveryService (unit):**
- [ ] POSTs payload as JSON to subscriber URL
- [ ] Includes `X-Webhook-Signature` HMAC-SHA256 header
- [ ] Signature uses subscriber's secret and raw JSON body
- [ ] Marks delivery `delivered` on 2xx response
- [ ] Marks delivery `failed` and raises on non-2xx
- [ ] Marks delivery `failed` and raises on network error
- [ ] Sets `delivered_at` timestamp on success

**WebhookDeliveryJob (unit):**
- [ ] Calls service with loaded delivery record
- [ ] Is queued in correct Sidekiq queue
- [ ] Retry count matches existing worker pattern

**WebhookSubscriptionListener (unit):**
- [ ] Creates a WebhookDelivery record for each matching subscriber
- [ ] Enqueues WebhookDeliveryJob for each created delivery
- [ ] Skips subscribers for non-matching event types

**WebhookDelivery model (unit):**
- [ ] `mark_delivered!` transitions status, sets timestamp
- [ ] `mark_failed!` transitions status, increments attempt count
- [ ] Validates presence of event_type, payload, subscriber

**Integration:**
- [ ] Publishing an event results in HTTP POST to subscriber URL (VCR/WebMock)

---

## Preparatory Refactoring

Explicitly confirmed: **none needed.** Proceed directly to feature steps.

---

## Numbered Implementation Steps

---

### Step 1 — `WebhookDelivery` model: status field \u0026 state methods
**Tag:** `feat: add status and state transitions to WebhookDelivery`
**Files:** `db/migrate/`, `app/models/webhook_delivery.rb`, `spec/models/webhook_delivery_spec.rb`

**Behavioral expectations:**
- Model has `status` (default: `pending`), `delivered_at`, `attempt_count`
- `mark_delivered!` sets status → `delivered`, stamps `delivered_at`
- `mark_failed!` sets status → `failed`, increments `attempt_count`

**Acceptance criteria:**
- Migration runs cleanly, rollback clean
- All three model unit tests pass
- No existing tests broken

**Definition of done:** Tests green + migration reversible

---

### Step 2 — `WebhookDeliveryService`: HTTP POST + HMAC signature
**Tag:** `feat: add WebhookDeliveryService with HTTP delivery and HMAC signing`
**Files:** `app/services/webhook_delivery_service.rb`, `spec/services/webhook_delivery_service_spec.rb`

**Behavioral expectations:**
- POSTs JSON body to `delivery.subscriber.url`
- `X-Webhook-Signature: sha256=\u003chmac>` header present and correct
- 2xx → calls `mark_delivered!`
- Non-2xx → calls `mark_failed!`, raises (enabling Sidekiq retry)
- Network error → calls `mark_failed!`, re-raises

**Acceptance criteria:**
- All six service unit tests pass (WebMock stubs — no real HTTP)
- HMAC verified against known test vector in spec

**Definition of done:** Tests green, no real HTTP calls possible in test suite

---

### Step 3 — `WebhookDeliveryJob`: Sidekiq worker
**Tag:** `feat: add WebhookDeliveryJob following existing worker pattern`
**Files:** `app/jobs/webhook_delivery_job.rb`, `spec/jobs/webhook_delivery_job_spec.rb`

**Behavioral expectations:**
- `perform(delivery_id)` loads record, delegates to `WebhookDeliveryService#call`
- Queued in the queue matching existing worker convention (confirm at DO time)
- Retry count matches existing pattern (confirm at DO time)
- Raised exceptions propagate (Sidekiq handles retry)

**Acceptance criteria:**
- Job unit tests pass with service stubbed
- Queue name and retry count verified against existing job

**Definition of done:** Tests green, pattern consistent with existing jobs

---

### Step 4 — `WebhookSubscriptionListener`: EventBus integration
**Tag:** `feat: add WebhookSubscriptionListener and subscribe to EventBus`
**Files:** `app/listeners/webhook_subscription_listener.rb`, `spec/listeners/webhook_subscription_listener_spec.rb`, EventBus subscription initializer (confirm location at DO time)

**Behavioral expectations:**
- `handle(event_type, payload)` finds all subscribers for `event_type`
- Creates `WebhookDelivery` per subscriber
- Enqueues `WebhookDeliveryJob` per delivery
- Subscribers for other event types are not processed

**Acceptance criteria:**
- Listener unit tests pass with job stubbed
- EventBus subscription wired in initializer (confirm pattern at DO time)
- Existing EventBus tests unaffected

**Definition of done:** Tests green, subscription confirmed in console/test

---

### Step 5 — Integration test: publish → HTTP POST
**Tag:** `test: integration coverage for event publish through HTTP delivery`
**Files:** `spec/integration/webhook_delivery_spec.rb` (or `spec/features/` — match existing convention)

**Behavioral expectations:**
- Publishing an event results in a stubbed HTTP POST to subscriber URL
- Delivery record transitions to `delivered`
- Signature header present and verifiable

**Acceptance criteria:**
- Integration test passes with WebMock
- Full stack exercised without mocking internal seams

**Definition of done:** Tests green end-to-end

---

### Step 6 — Documentation
**Tag:** `docs: update README and CLAUDE.md for webhook delivery`
**Files:** `README.md`, `CLAUDE.md`

**Explicit tasks (not implied):**
- [ ] README: document webhook subscription model, HMAC secret setup, event types
- [ ] CLAUDE.md commands table: add any new rake tasks, test commands, or scripts introduced

**Acceptance criteria:**
- Another developer can configure and test webhook delivery from docs alone

---

## Risk Areas to Monitor

| Risk | Mitigation |
|---|---|
| HMAC secret storage — where does `subscriber.secret` live? | Confirm at Step 2 DO time; flag if not encrypted at rest |
| EventBus subscription location — initializer vs. engine? | Confirm pattern before Step 4 |
| Sidekiq queue name — hardcoded or per-env config? | Check existing jobs in Step 3 |
| HTTP client choice — Net::HTTP vs. Faraday already in stack? | Check Gemfile at Step 2; use what's already there |
| `delivered_at` timezone — use `Time.current` not `Time.now` | Enforce in Step 1 model spec |

---

## Rollback Approach

- Steps 1–4 are independently revertable: migration rollback, file deletion, remove subscription line
- No changes to existing model behavior until Step 4 wires the listener
- Feature flag option: wrap EventBus subscription in `Rails.configuration.enable_webhooks` if needed

---

## CHECK Criteria (per step)

Each step is complete when:
1. The specific behavioral tests written in that step pass
2. Full test suite passes (`bundle exec rspec`)
3. Commit message matches tag convention
4. No new test is skipped or pending without explanation

---

## ACT (Post-Implementation Retrospective)

After Step 5 passes:
- Was the service/job/listener separation the right granularity?
- Did the HMAC approach match what subscribers will expect? (Consider: timestamp in signature to prevent replay attacks — defer or add?)
- Proposed working agreement update: *\"Integration tests for async flows use WebMock + real job execution (not `have_enqueued_job` only)\"*

---

> **To execute each step, invoke the PDCA Do prompt. Do not begin any step without first opening that prompt.**
```

### 1b-needs-refactoring (Planning)

**GEval:** 0.90 / threshold 0.50 ✅

**Mechanical checks:**

- ✅ `must_contain: 'refactor'` — 'refactor' found in output
- ✅ `must_contain: 'feat'` — 'feat' found in output
- ✅ `must_not_contain: 'Here is the complete implementation'` — 'Here is the complete implementation' absent (good) in output
- ✅ `must_not_contain: 'skip the refactor'` — 'skip the refactor' absent (good) in output

**Judge reasoning:**

> The Actual Output demonstrates strong alignment with all six evaluation steps. Step 1 is fully satisfied: all steps are numbered and atomic, each describing exactly one testable behavior change (e.g., Step 1 extracts PaymentProcessor, Step 4 scopes it to TenantContext—these are separate concerns). Step 2 is met: a comprehensive \"Test List\" section enumerates golden path (e.g., \"Processes payment successfully for valid tenant\"), degenerate cases (\"empty/zero results for tenant with no data\"), and exceptions (\"Rejects operation with missing/invalid TenantContext\"), presented as an explicit planning artifact. Step 3 is satisfied: both `refactor:` steps (1 and 2) appear before all `feat:` steps (3–6), and each includes acceptance criteria confirming \"all existing tests pass unchanged.\" Step 4 is met: no runnable code is present—only structural references like method names (\"Add TenantContext parameter\"), schema columns (\"WHERE tenant_id = ?\"), class names (PaymentProcessor, ReportingService, TenantContext), and ASCII diagrams. Step 5 is fully satisfied: every step includes explicit acceptance criteria and a definition of done that is independently testable (e.g., \"Test cases explicitly assert cross-tenant data isolation\"). Step 6 is correctly addressed: the plan spans 4+ files (BillingService, PaymentProcessor, ReportingService, TenantContext, plus test files) and introduces a new architectural pattern (TenantContext as a scoping abstraction), and the output explicitly states at the end: \"This plan spans 4+ files and introduces a new architectural pattern (TenantContext). Consider requesting an adversarial critic pass on this analysis before implementation begins.\" Minor weakness: the \"Preparatory Refactoring Steps\" section uses unchecked confirmation boxes that could be slightly clearer in structure, but this does not materially affect the plan's rigor.

**Input:**

```
Analysis complete. Adding multi-tenant support to our billing module. Problem: BillingService is a 600-line God object mixing tenant logic, payment processing, and reporting. We need to extract PaymentProcessor and ReportingService before adding tenant scoping. Approach chosen: extract-then-scope pattern.
```

**Model output:**

```
# Planning Phase: BillingService Decomposition + Multi-Tenant Scoping

## Integration Strategy

### End-to-End Data Flow \u0026 Touch Points

```
Current:
[API Layer] → BillingService (600-line God object)
                ├── tenant logic
                ├── payment processing  
                └── reporting

Target:
[API Layer] → BillingService (thin orchestrator + tenant scoping)
                ├── PaymentProcessor (extracted)
                └── ReportingService (extracted)
              ↑
          TenantContext (new scoping layer)
```

**Touch points to map before implementation begins:**
- All callers of BillingService (API controllers, background jobs, other services)
- Any direct instantiation vs. dependency injection
- Database queries — are tenant IDs already present in schema or absent?
- Configuration/initialization sites

---

## Preparatory Refactoring Steps

These come **before** any `feat:` work. All existing tests must remain green after each.

- [ ] Confirm: Does BillingService have adequate test coverage to safely refactor? If not, add characterization tests first (explicit `refactor:` step 0).
- [ ] Confirm: Is DI wiring in place, or is BillingService instantiated directly? Plan injection strategy accordingly.

---

## Implementation Plan

### STEP 0 — Characterization Tests (if coverage is insufficient)
**Tag:** `test:` | **Complexity:** Medium  
**Behavioral expectation:** Existing behavior is documented via tests before any structural change  
**Acceptance criteria:**
- Tests cover the three responsibility zones: payment flows, reporting outputs, tenant-affecting paths
- All tests pass on current code
- No production code changed

**CHECK:** Coverage report shows key paths covered; no regressions

---

### STEP 1 — Extract PaymentProcessor (structural, no behavioral change)
**Tag:** `refactor:`| **Complexity:** Medium  
**Behavioral expectation:** Payment processing behavior is identical; BillingService delegates to PaymentProcessor  
**Steps:**
1. Identify all payment-related methods in BillingService
2. Create `PaymentProcessor` class with those methods moved verbatim
3. BillingService holds a reference and delegates — no logic changes
4. Update DI wiring if applicable

**Acceptance criteria:**
- All existing tests pass unchanged
- No payment logic remains directly in BillingService
- PaymentProcessor has its own unit test file (even if initially thin)

**CHECK:** `grep` for payment method names confirms they no longer live in BillingService; test suite green

---

### STEP 2 — Extract ReportingService (structural, no behavioral change)
**Tag:** `refactor:` | **Complexity:** Medium  
**Behavioral expectation:** Reporting behavior identical; BillingService delegates to ReportingService  
**Steps:**
1. Identify all reporting-related methods in BillingService
2. Create `ReportingService` with those methods moved verbatim
3. BillingService delegates — no logic changes
4. Update DI wiring

**Acceptance criteria:**
- All existing tests pass unchanged
- No reporting logic remains directly in BillingService
- ReportingService has its own unit test file

**CHECK:** BillingService is now a thin orchestrator; line count dropped significantly; test suite green

---

### STEP 3 — Introduce TenantContext (new abstraction)
**Tag:** `feat:` | **Complexity:** Low-Medium  
**Behavioral expectation:** A TenantContext object carries tenant identity and can be passed through the system  
**Steps:**
1. Define `TenantContext` (struct/class/value object — match existing patterns)
2. No integration yet — just the type and any validation logic
3. Unit test: construction, validation, equality

**Acceptance criteria:**
- TenantContext is tested in isolation
- No existing code modified
- Follows existing patterns for value objects/context objects in this codebase

**CHECK:** New file only; zero changes to existing files; tests green

---

### STEP 4 — Scope PaymentProcessor to TenantContext
**Tag:** `feat:` | **Complexity:** Medium  
**Behavioral expectation:** PaymentProcessor methods accept/require TenantContext; queries/operations are tenant-scoped  
**Steps:**
1. Add TenantContext parameter to PaymentProcessor methods
2. Apply tenant scoping to DB queries (WHERE tenant_id = ?)
3. Update PaymentProcessor tests to supply TenantContext
4. Update BillingService delegation calls

**Acceptance criteria:**
- PaymentProcessor cannot operate without a valid TenantContext
- Tests verify tenant isolation (tenant A cannot see tenant B data)
- Existing integration tests updated and passing

**CHECK:** Test cases explicitly assert cross-tenant data isolation; no unscoped queries remain in PaymentProcessor

---

### STEP 5 — Scope ReportingService to TenantContext
**Tag:** `feat:` | **Complexity:** Medium  
**Behavioral expectation:** Reporting data is tenant-scoped; no cross-tenant data leakage  
**Steps:**
1. Add TenantContext parameter to ReportingService methods
2. Apply tenant scoping to all report queries
3. Update ReportingService tests
4. Update BillingService delegation calls

**Acceptance criteria:**
- Reports contain only data belonging to the specified tenant
- Tests verify isolation with multi-tenant fixture data
- All existing tests pass

**CHECK:** Multi-tenant test fixtures confirm reports don't bleed across tenants; test suite green

---

### STEP 6 — Propagate TenantContext through BillingService API
**Tag:** `feat:` | **Complexity:** Medium  
**Behavioral expectation:** BillingService public interface accepts TenantContext and propagates it to both extracted services  
**Steps:**
1. Update BillingService public methods to accept TenantContext
2. Pass context down to PaymentProcessor and ReportingService
3. Update all callers (API layer, background jobs, other services)
4. Update integration tests

**Acceptance criteria:**
- No caller can invoke BillingService without supplying TenantContext
- End-to-end tests cover the full call chain with tenant scoping
- Background jobs supply tenant context correctly

**CHECK:** Grep for all BillingService call sites; confirm all updated; no context dropped in the chain

---

### STEP 7 — Documentation \u0026 Build System
**Tag:** `docs:` | **Complexity:** Low  
**Explicit tasks (not implied):**
- [ ] README updated: document new three-class structure and tenant scoping contract
- [ ] CLAUDE.md commands table updated with any new build/test commands introduced
- [ ] If DI container config changed: document initialization order
- [ ] Add architecture decision note: why extract-then-scope pattern was chosen

**CHECK:** README describes the new module structure; CLAUDE.md is current; no undocumented commands

---

## Test List (Full Behavioral Inventory)

**PaymentProcessor:**
- Processes payment successfully for valid tenant
- Rejects operation with missing/invalid TenantContext
- Tenant A payment cannot affect Tenant B records
- Payment failure handling (gateway errors, insufficient funds)
- Idempotency behavior (if applicable)

**ReportingService:**
- Returns correct report data for tenant
- Returns empty/zero results for tenant with no data (not another tenant's data)
- Date range filtering works within tenant scope
- Aggregations are tenant-isolated

**TenantContext:**
- Constructs with valid tenant ID
- Rejects invalid/empty tenant ID
- Equality semantics correct

**BillingService (integration):**
- End-to-end: API call → BillingService → PaymentProcessor (tenant-scoped)
- End-to-end: API call → BillingService → ReportingService (tenant-scoped)
- Background job supplies correct TenantContext
- Multi-tenant: concurrent operations for different tenants don't interfere

---

## Risk Areas

| Risk | Mitigation |
|------|-----------|
| Hidden coupling in God object | Characterization tests before any extraction |
| DB schema missing tenant_id columns | Validate schema before Step 4 — may require migration |
| Background jobs with no tenant concept | Audit all job sites in Step 6 |
| Callers assuming single-tenant behavior | Compiler/type errors will surface them if TenantContext is required type |
| Transaction boundaries crossing the extraction | Map transaction scope before Step 1 |

---

## Rollback Approach

- Steps 1–2 are pure refactors: revert commits, tests confirm equivalence
- Steps 3–6 are additive: feature flags can gate the tenant-scoped paths if needed
- Each step is a clean commit boundary — revert is a single `git revert`

---

## Process Checkpoints

- **Complexity check:** Steps 4 and 5 may feel large if DB migration is required — split migration into its own `chore:` step if so
- **Critic pass:** This plan spans 4+ files and introduces a new architectural pattern (TenantContext). Consider requesting an adversarial critic pass on this analysis before implementation begins.

---

> **Reminder:** To execute each step, invoke the **PDCA Do** prompt. Do not begin any step without first opening that prompt. Each step is one Do cycle — resist the urge to batch steps.
```

### 1b-multi-system (Planning)

**GEval:** 0.90 / threshold 0.50 ✅

**Mechanical checks:**

- ✅ `must_contain: 'Node'` — 'Node' found in output
- ✅ `must_contain: 'Python'` — 'Python' found in output
- ✅ `must_not_contain: 'Here is the code'` — 'Here is the code' absent (good) in output
- ✅ `must_not_contain: 'npm install'` — 'npm install' absent (good) in output

**Judge reasoning:**

> The output demonstrates exceptional alignment with all six evaluation steps. (1) Steps are numbered and atomic—each introduces exactly one testable behavior (e.g., Step 1 creates the Redis writer module, Step 3 calls it, Step 4 implements the consumer). (2) A comprehensive Test List is provided as a separate planning artifact, enumerating 16 behaviors organized by Python, Node.js consumer, cache, API route, and integration layers, covering golden paths, degenerate cases (malformed entries, empty cache), and exception handling (connection failure, restart recovery). (3) Preparatory refactoring (Steps 1–2, tagged `refactor:`) explicitly precedes feature work (Steps 3–6, tagged `feat:`), and includes assertions that \"existing tests still pass\" after each refactor step. (4) No executable code appears; only method signatures (`write(stream_name: str, fields: dict)`, `start()`, `stop()`), schema references, and an ASCII data-flow diagram provide context without implementation. (5) Every step includes explicit acceptance criteria and definition of done (e.g., Step 2: \"unit tests for TTL behavior pass\"; Step 3: \"test verifies `XADD` called with correct args\"). (6) The plan spans Python and Node.js services with a new Redis stream architectural pattern and explicitly raises unresolved architectural decisions (consumer group vs. simple XREAD, stream maxlen, cache key structure, empty cache response), though it does not explicitly ask whether an adversarial critic pass is desired before implementation—a minor gap. The risk table, rollback approach, and retrospective checkpoint (Step 10) further strengthen the output. The one limitation is the conditional framing (\"pending pattern confirmation\") and the architectural discovery phase, which, while rigorous, slightly defer the core plan rather than presenting it as fully formed.

**Input:**

```
Analysis complete. We need to add real-time usage metrics that flow from our Python data pipeline into our Node.js API for display. Integration point: shared Redis stream. Chosen approach: Python writes metrics events to Redis stream, Node.js consumer reads and caches in memory with 30s TTL.
```

**Model output:**

```
# Planning Phase: Real-Time Usage Metrics via Redis Stream

## Mandatory First Step: Architecture Pattern Discovery

Before I can produce a reliable plan, I need to validate the existing codebase patterns. Since no codebase has been shared in this conversation, I'll do what the prompt requires: **list the concrete files and search targets I would inspect before proceeding**, rather than fabricate findings.

---

## Architecture Pattern Discovery (Required Before Planning)

**SEARCH 1 — Similar feature implementations:**
- Query: *\"How does existing event publishing or stream writing work in the Python pipeline?\"*
- Target files/patterns: `pipeline/`, `producers/`, `writers/`, `emitters/`, any file matching `*stream*`, `*publish*`, `*redis*`

**SEARCH 2 — Integration patterns (Node.js side):**
- Query: *\"Where does the Node.js API consume external data sources or background jobs?\"*
- Target files/patterns: `src/consumers/`, `src/cache/`, `src/middleware/`, `server.js`, `app.js`, any file matching `*redis*`, `*cache*`, `*worker*`

**SEARCH 3 — Configuration patterns:**
- Query: *\"How are Redis connection settings, TTL values, and stream names configured across both services?\"*
- Target files/patterns: `config/`, `.env`, `settings.py`, `config.js`, `docker-compose.yml`, any shared config or secrets pattern

**STOP CONDITION — I need answers to these before the plan can be concrete:**

1. **Python side:** Does an existing Redis client/connection abstraction exist, or is this the first Redis usage? Is there an existing event/metric emission pattern to follow (e.g., structured log emitter, webhook publisher)?
2. **Node.js side:** Is there an existing background worker or polling loop pattern, or will this be the first? What in-memory cache pattern exists (plain `Map`, a cache library, a singleton service)?
3. **Shared:** Is the Redis stream name, event schema, and connection config already agreed upon, or does that need to be defined here?
4. **External system validation:** Has a real Redis XADD/XREAD round-trip been tested in this environment? What does the actual event payload look like?

---

## Conditional Plan (Pending Pattern Confirmation)

*The following is structured around the chosen approach but should be revised once the above searches are complete. Steps marked ⚠️ carry assumptions that must be validated.*

---

### Integration Map: End-to-End Data Flow

```
Python Pipeline
  └─ [metric event occurs]
       └─ RedisStreamWriter.write(event)  →  XADD metrics:stream * field value ...
                                                        │
                                                   Redis Stream
                                                        │
Node.js API                                             ▼
  └─ MetricsConsumer (background)  ←  XREAD BLOCK 0 STREAMS metrics:stream >
       └─ normalize \u0026 validate event
            └─ MetricsCache.set(key, value, ttl=30s)
                 └─ [API route handler]  ←  MetricsCache.get(key)  →  HTTP response
```

Touch points requiring changes to existing code:
- Python: one new write call at the metric emission site
- Node.js: server startup must launch consumer; API route must read from cache instead of (or in addition to) whatever current source exists

---

### Preparatory Refactoring

- [ ] **Python:** If no Redis client wrapper exists, a thin connection/writer module must be created before behavioral steps begin — tagged `refactor:` since it adds no behavior on its own
- [ ] **Node.js:** If no cache abstraction exists, extract one before wiring the consumer — keeps consumer logic testable in isolation
- [ ] Confirm: are existing tests passing before any work begins? (baseline)
- [ ] If both services already have Redis clients and a cache layer: **confirm structure is ready as-is** and skip preparatory steps

---

### Test List (All Behaviors to Verify — Execution is One at a Time)

**Python:**
1. `write()` calls `XADD` with the correct stream name and field structure
2. `write()` on Redis connection failure raises a catchable, typed exception (does not silently drop)
3. Metric fields are validated/typed before write (no bare strings where numbers expected)
4. ⚠️ Stream trimming/maxlen is applied if agreed upon (prevents unbounded growth)

**Node.js consumer:**
5. Consumer reads a new entry from the stream and stores it in cache
6. Consumer ignores/logs malformed entries without crashing
7. Consumer resumes from last-read ID after restart (not from `0`, not missing events) ⚠️ requires confirming ID persistence strategy
8. Consumer stops cleanly on process shutdown signal

**Node.js cache:**
9. `get()` returns value within 30s TTL
10. `get()` returns `null`/`undefined` after TTL expires
11. `set()` overwrites stale value for same key
12. Cache is accessible from route handler without circular dependencies

**Node.js API route:**
13. Route returns cached metrics when available
14. Route returns a defined \"no data yet\" response (not 500) when cache is empty
15. Route returns stale-data indicator if TTL recently expired ⚠️ only if product requires this

**Integration (optional, if environment supports it):**
16. Full round-trip: Python write → Redis → Node.js read → route response

---

### Implementation Steps

**Step 0 — Baseline**
- Run full test suite on both services; confirm green
- Acceptance: all existing tests pass
- Tag: `chore:`

---

**Step 1 — Preparatory: Python Redis writer module** *(skip if abstraction exists)*
- Create `metrics/redis_writer.py` (or equivalent path matching project conventions)
- Exposes `write(stream_name: str, fields: dict) -> None`
- Uses existing Redis client pattern if one exists; otherwise establishes the project's first
- No behavioral change to pipeline yet
- Acceptance: module importable, existing tests still pass
- Tag: `refactor:`

---

**Step 2 — Preparatory: Node.js cache module** *(skip if abstraction exists)*
- Create `src/cache/metricsCache.js` (or match project naming convention)
- Implements `set(key, value, ttlSeconds)` and `get(key)` using plain `Map` + `setTimeout` or existing cache library
- Exported as singleton
- Acceptance: unit tests for TTL behavior pass; no integration with consumer yet
- Tag: `refactor:`

---

**Step 3 — Python: write metric event to stream**
- At the identified metric emission site in the pipeline, add call to `writer.write('metrics:stream', event_fields)`
- Define and document the event schema (field names, types) as a comment or schema file — this is the contract
- Acceptance: test verifies `XADD` called with correct args (mock Redis); pipeline runs without error
- Tag: `feat:`

---

**Step 4 — Node.js: consumer reads stream and populates cache**
- Create `src/consumers/metricsConsumer.js`
- Implements `start()`: runs `XREAD BLOCK` loop, parses entries, calls `metricsCache.set()`
- Implements `stop()`: breaks loop cleanly
- Persists last-read stream ID (in-memory for now; upgrade to Redis key if restart-survival is required)
- Acceptance: unit tests for steps 5–8 in test list pass (mock Redis client)
- Tag: `feat:`

---

**Step 5 — Node.js: wire consumer into server startup**
- In `server.js` / `app.js`, call `metricsConsumer.start()` after server ready
- Register `metricsConsumer.stop()` on `SIGTERM`/`SIGINT`
- Acceptance: server starts and stops cleanly; no test regression
- Tag: `feat:`

---

**Step 6 — Node.js: API route reads from cache**
- Add or modify route handler to call `metricsCache.get(key)` and return result
- Define response contract for empty-cache case (e.g., `204`, `{ data: null, status: 'pending' }`)
- Acceptance: route tests for steps 13–14 pass
- Tag: `feat:`

---

**Step 7 — Configuration: externalize stream name and TTL**
- Move `'metrics:stream'`, `30` (TTL), and Redis connection settings to config/env
- Both Python and Node.js read from environment variables using existing config patterns
- Acceptance: both services configurable without code change; values documented in README/.env.example
- Tag: `chore:`

---

**Step 8 — Documentation**
- Update `README.md`: describe the metrics flow, stream name, event schema, TTL behavior
- Update `CLAUDE.md` commands table if any new scripts were added (e.g., a stream debug/inspect command)
- Acceptance: a new developer can understand the data flow from docs alone without reading code

---

**Step 9 — CHECK**
- Full test suite passes on both services
- Redis stream maxlen/trimming strategy confirmed or explicitly deferred with a logged decision
- Event schema is documented and both sides agree on field names and types
- No unbounded memory growth path in the Node.js cache (TTL expiry actually frees memory — verify with the chosen implementation)
- Restart behavior tested: does the consumer pick up where it left off or re-read from scratch? Is that acceptable?

---

**Step 10 — ACT (Retrospective, 5–10 min)**
- What assumptions turned out wrong?
- Was the Redis stream the right integration point, or did we discover a simpler shared mechanism?
- Should the \"no data yet\" API contract be refined based on what the frontend actually needs?
- Proposed working agreement updates: stream naming convention, event schema versioning approach

---

## Risk Areas

| Risk | Likelihood | Mitigation |
|------|-----------|-----------|
| Redis not available in test environment | Medium | Abstract client behind interface; mock in unit tests |
| Stream grows unbounded if consumer lags | Medium | Add `MAXLEN ~` to `XADD`; define in Step 3 |
| Consumer restarts lose read position | Medium | Decide now: is restart-from-zero acceptable or must we persist consumer group/ID? |
| Event schema mismatch between Python and Node.js | High | Document schema as explicit contract artifact before Step 3 |
| Memory leak in Node.js cache | Medium | Verify TTL implementation actually deletes from `Map`, not just marks stale |

---

## Rollback Approach

- Python: the `writer.write()` call is additive; wrap in a try/except that logs and continues — pipeline never blocks on metrics
- Node.js: consumer failure does not affect API availability; route returns \"no data\" response, not 500
- Both sides independently deployable; neither blocks the other

---

## Unresolved Decisions (Must Answer Before Step 3)

1. **Consumer group vs. simple XREAD?** Consumer group allows multiple readers and acknowledged delivery; simple XREAD is simpler but doesn't track position across restarts. Which do we need?
2. **Stream maxlen?** What's the acceptable lag before old events are discarded?
3. **Cache key structure?** Is it one key for all metrics, or per-metric-type keys?
4. **Empty cache response?** What does the frontend expect when no data has arrived yet?

---

> **To execute each step, invoke the PDCA Do prompt. Do not begin any step without first opening that prompt.**
```

### 1b-critic-pass-warranted (Planning)

**GEval:** 0.90 / threshold 0.50 ✅

**Mechanical checks:**

- ✅ `must_not_contain: 'Here is the complete implementation'` — 'Here is the complete implementation' absent (good) in output
- ✅ `must_not_contain: 'def replay_events'` — 'def replay_events' absent (good) in output

**Judge reasoning:**

> The response demonstrates strong alignment with all six evaluation steps. Step 1 is fully satisfied: each numbered step describes exactly one testable behavior change with no combining of independent modifications (e.g., Step 1 focuses solely on schema/persistence, Step 2 on sequence integrity, Step 3 on event creation). Step 2 is comprehensively met with a detailed test list that enumerates golden path cases (event persistence, correct sequencing), degenerate cases (empty event stream, concurrent appends, unknown event type), and exception scenarios (missing fields, transaction rollback), presented as a distinct planning artifact separate from implementation. Step 3 is correctly applied: there are no refactor: steps present, which is appropriate since the input explicitly states \"no preparatory refactoring identified.\" Step 4 is satisfied with no runnable code provided—only method names, schema field references ('order_events' table structure), and structural concepts (worker pattern). Step 5 is fully addressed: each numbered step includes explicit acceptance criteria (checkboxes) and clear definition of done (e.g., \"Tests pass, migration checked in, no existing tests broken\"). Step 6 is properly executed: the plan spans 4 touch points and introduces a new architectural pattern (event sourcing to a CRUD-only codebase), and the output explicitly raises the adversarial critic pass recommendation before implementation begins. The risk table and decision checkpoints (sequence assignment strategy in Step 2, unknown event type behavior in Step 5) further strengthen the plan's thoroughness. Minor areas for improvement: the critic pass prompt could be more assertively positioned as a gating requirement rather than a recommendation, and the CLAUDE.md reference assumes context not fully defined in the evaluation.

**Input:**

```
Analysis complete. We're adding an event-sourcing audit trail to our order-processing system. This is a new pattern for this codebase, which is CRUD-only today. Plan touches: OrderService (existing), a new EventStore model, a new event-replay worker, and the OrdersController serializer. No existing precedent for event sourcing here, and no preparatory refactoring identified — the new pieces are additive.
```

**Model output:**

```
# Implementation Plan: Event-Sourcing Audit Trail

**Execution Context:** TDD discipline, one failing test at a time, human-supervised steps. New architectural pattern — additive to existing CRUD system.

---

## Pre-Implementation: Critic Pass Recommendation

⚠️ **This plan introduces a new architectural pattern across 4 touch points with no existing precedent.** Before implementation begins, I recommend an adversarial critic pass on this plan. Would you like me to argue against this design before you approve it?

---

## Test List (Planning Artifact — Not Execution Order)

Enumerate all behaviors to verify:

**EventStore model**
- Persists an event with aggregate type, aggregate id, event type, payload, and timestamp
- Rejects an event missing required fields
- Orders events by sequence within an aggregate

**OrderService integration**
- Appends an `order_created` event when an order is created
- Appends an `order_updated` event when an order is updated
- Appends an `order_cancelled` event when an order is cancelled
- Does not persist the order if event append fails (transactional boundary)

**Event-replay worker**
- Replays events in sequence to reconstruct order state
- Produces identical state to the CRUD record for a known fixture
- Handles an empty event stream gracefully
- Handles an unknown event type gracefully (skip or raise — decision needed)

**OrdersController serializer**
- Includes audit trail (event list) in order response when requested
- Excludes audit trail by default (no breaking change to existing response shape)

---

## Numbered Implementation Steps

---

### Step 1 — `EventStore` model: schema and basic persistence
`feat:` | **Low complexity**

**What to build:** Migration + model for `order_events` table.

Fields: `id`, `aggregate_type` (string), `aggregate_id` (string/uuid), `event_type` (string), `payload` (jsonb), `sequence` (integer), `occurred_at` (timestamp).

**Acceptance criteria:**
- [ ] Migration runs cleanly
- [ ] Model validates presence of `aggregate_type`, `aggregate_id`, `event_type`, `payload`
- [ ] Events are retrievable ordered by `sequence` scoped to an aggregate

**Test:** One failing model validation test first, then persistence test.

**Definition of done:** Tests pass, migration checked in, no existing tests broken.

---

### Step 2 — `EventStore` sequence integrity
`feat:` | **Low complexity**

**What to build:** Auto-increment `sequence` within an aggregate on write (database-level or model callback — decide which before writing the test).

> ⚠️ **Decision checkpoint:** Sequence assignment strategy must be chosen before this step. Options: DB sequence, optimistic lock counter, max+1 query. Wrong choice here causes replay bugs. Recommend DB-native sequence or row-level locking. Flag for operator approval.

**Acceptance criteria:**
- [ ] Two events appended to the same aggregate get sequence 1 and 2 respectively
- [ ] Events on different aggregates do not interfere with each other's sequences

**Test:** Concurrent-append behavior (degenerate case) should be noted even if not fully tested at unit level.

**Definition of done:** Tests pass, decision documented in a code comment.

---

### Step 3 — `OrderService`: append event on order creation
`feat:` | **Medium complexity**

**What to build:** After a successful `Order` save in `create`, append an `order_created` event to `EventStore`. Wrap both in a transaction.

**Acceptance criteria:**
- [ ] `order_created` event exists in `EventStore` after a successful create
- [ ] If event append raises, the order is not persisted (transaction rolls back)
- [ ] Existing `OrderService#create` tests still pass

**Test:** Write the failing integration test (order + event in transaction) before touching `OrderService`.

**Definition of done:** Tests pass, no change to `OrderService` public interface.

---

### Step 4 — `OrderService`: append event on order update and cancel
`feat:` | **Low complexity** (pattern established in Step 3)

**What to build:** Same transactional pattern for `update` → `order_updated`, and `cancel` → `order_cancelled`.

**Acceptance criteria:**
- [ ] Correct event type appended for each operation
- [ ] Transactional rollback behavior consistent with Step 3

**Test:** One failing test per operation before implementation.

**Definition of done:** All three event types covered, no existing tests broken.

---

### Step 5 — Event-replay worker: reconstruct order state from events
`feat:` | **High complexity — flag if expanding scope mid-step**

**What to build:** A worker (class or service object) that accepts an `aggregate_id`, loads its events in sequence, and folds them into an order state hash/struct.

> ⚠️ **Unknown event type behavior must be decided before this step.** Options: raise, skip with warning, halt replay. This is a correctness boundary. Recommend raising with a descriptive error and logging. Flag for operator approval.

**Acceptance criteria:**
- [ ] Replayed state matches CRUD record for a known fixture (golden path)
- [ ] Empty event stream returns nil or an explicit \"no events\" result (not a crash)
- [ ] Unknown event type raises `UnknownEventType` (or skips, per decision above)

**Test:** Start with the empty stream degenerate case, then golden path fixture, then unknown event type.

**Definition of done:** Tests pass, replay result documented with example output shape.

---

### Step 6 — `OrdersController` serializer: opt-in audit trail
`feat:` | **Low complexity**

**What to build:** When `?include=audit_trail` (or equivalent) is present in the request, serialize the event list alongside the order. Default response shape unchanged.

**Acceptance criteria:**
- [ ] Default response contains no event data (no breaking change)
- [ ] `?include=audit_trail` response includes ordered event list with type, payload, occurred_at
- [ ] Serializer does not call `EventStore` on requests without the flag

**Test:** Write failing request spec for default (no events) first, then opt-in case.

**Definition of done:** Existing controller tests pass unchanged.

---

### Step 7 — Documentation and build artifacts
`docs:` | **Low complexity — explicit, not implied**

- [ ] `README` updated: describe event-sourcing audit trail, how to query events, how replay works
- [ ] `CLAUDE.md` commands table updated with any new scripts (migration runner, replay invocation)
- [ ] Event type vocabulary documented (what payload shape each event type carries)

**Acceptance criteria:**
- [ ] Another engineer can understand the pattern from the docs without reading the implementation
- [ ] All commands in `CLAUDE.md` are executable and produce described output

---

## Risk Areas

| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| Sequence assignment race condition | Medium | Decide and lock in DB-level strategy in Step 2 |
| Transaction scope creep (event store + order) | Medium | Explicit transaction boundary test in Step 3 |
| Replay diverges from CRUD state | High (over time) | Golden-path fixture test in Step 5; revisit if order schema evolves |
| Serializer performance (N+1 on event load) | Medium | Eager-load events in serializer; test in Step 6 |
| Breaking existing API consumers | Low | Default response shape unchanged by design |

---

## Rollback Approach

Steps are additive. If any step must be reverted:
- Steps 1–2: Drop migration, remove model. No existing code touched.
- Steps 3–4: Remove event appends from `OrderService`. CRUD behavior unchanged.
- Step 5: Remove worker. No external exposure.
- Step 6: Remove serializer flag. Default response already untouched.

Feature flag wrapping the `OrderService` event appends (Steps 3–4) is worth considering if you need a production kill switch.

---

## CHECK Criteria (End of Plan)

After all steps, verify:
- [ ] All test list behaviors from the planning artifact are covered by tests
- [ ] Migration is reversible
- [ ] No existing `OrderService` or `OrdersController` tests are broken
- [ ] Replay produces state identical to CRUD for known fixtures
- [ ] `CLAUDE.md` and `README` updated and accurate
- [ ] Decision points (sequence strategy, unknown event type) are documented in code

## ACT (Retrospective Trigger)

After implementation: 5–10 min retrospective on whether the transaction boundary and replay worker met expectations. Propose any working agreement updates (e.g., \"all new event types must be registered in a central vocabulary file before use\").

---

> **Reminder:** To execute each step, invoke the **PDCA Do** prompt. Do not begin any step without first opening that prompt.
```
